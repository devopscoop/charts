# Brewfile for charts
#
# Installs every CLI tool used or referenced by this repo.
# Usage: brew bundle

# bash - pipeline.sh uses `#!/usr/bin/env bash`
brew "bash"

# git - cloning and the contribution workflow
brew "git"

# helm - linting, templating, packaging, and installing the charts
brew "helm"
